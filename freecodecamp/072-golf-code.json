{"index.js":"var names = [\"Hole-in-one!\", \"Eagle\", \"Birdie\", \"Par\", \"Bogey\", \"Double Bogey\", \"Go Home!\"];\nfunction golfScore(par, strokes) {\n  // Only change code below this line\n  // I want to use a loop but we don't know how to do this yet so I don't think\n  // that's what they're asking for\n  var nickname;\n  if (strokes == 1) {\n    nickname = names[0];\n  } else if (strokes <= par - 2) {\n    nickname = names[1];\n  } else if (strokes == par - 1) {\n    nickname = names[2];\n  } else if (strokes == par) {\n    nickname = names[3];\n  } else if (strokes == par + 1) {\n    nickname = names[4];\n  } else if (strokes == par + 2) {\n    nickname = names[5];\n  } else if (strokes >= par + 3) {\n    nickname = names[6];\n  } else {\n    nickname = \"Uh oh. The code is broken.\";\n  }\n  \n  return nickname;\n  // Only change code above this line\n}\n\n// Change these values to test\ngolfScore(5, 4);"}